# Generated by IBM TransformationAdvisor
# Fri Jun 21 08:52:31 UTC 2019

apiVersion: v1
kind: Service
metadata:
  name: icsbankwar
  labels:
    app: icsbankwar
spec:
  ports:
    - port: 9080
  selector:
    app: icsbankwar
  type: NodePort

---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  labels:
    app: icsbankwar
  name: icsbankwar
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: icsbankwar
    spec:
      containers:
      - image: mycluster.icp:8500/default/icsbankwar:latest
        name: icsbankwar
        env:
          - name: WLP_LOGGING_CONSOLE_FORMAT
            value: json
          - name: WLP_LOGGING_CONSOLE_LOGLEVEL
            value: info
          - name: WLP_LOGGING_CONSOLE_SOURCE
            value: message,trace,accessLog,ffdc
        ports:
        - containerPort: 9080
          protocol: TCP
